export interface Product {
	title: string;
	description: string;
	imageSource: string;
	url: string;
	color: string;
}

export interface Position {
	title: string;
	description: string;
	shortDescription: string;
	scope: string;
	slug: string;
}
